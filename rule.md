# 群内玩法规则

## 随机事件优先级

- 报名中或进行中的随机事件拥有最高优先级；同一时刻，其他玩法和普通指令都不能打断它。
- 报名中仅允许 `/加入 身份` 报名，以及已报名玩家使用 `/退出` 取消报名。
- 进行中仅允许参与玩家使用 `/退出` 退出并按轮次结算；参与玩家的普通发言用于记录事件轮次，旁观者只能以 `（内容）` 或 `(内容)` 形式发言。
- 除上述事件必需动作外，包含 `/打卡` 在内的所有指令均静默不执行、不产生奖励、不回复提示。
