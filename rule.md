# 摸鱼公司群机器人：全局规则

> 本文件是当前已实现玩法与经济系统的唯一规则基线。所有日期、跨日、定时任务与展示时间均以**北京时间（Asia/Shanghai）**为准。管理端的可配置值优先于下述默认值。

## 1. 基础身份与指令

- 玩家须先发送 `/入职 名字` 注册；同一平台账户只能拥有一名员工，重复入职不会覆盖原昵称或余额。
- 默认入职奖励为 `0` 摸鱼币，可在管理端配置。
- 当前基础查询指令：
  - `/我` 或 `/me`：余额、今日活跃等级、今日正向收益、连续打卡天数；
  - `/余额`：当前余额；
  - `/我的物品`：已拥有物品；
  - `/商店`：查看当前上架商品；
  - `/帮助`：查看启用的指令说明。
- 指令回复文案可在管理端编辑。模板变量由对应指令页面提供；编辑回复不会改变结算逻辑。

## 2. 经济总则

- 默认货币名称为“摸鱼币”，名称、入职奖励、打卡奖励与全勤奖均可配置。
- 余额允许为负数；惩罚、冻结与奖池扣除不会因余额不足而拒绝执行。
- 每一次余额变动都会记账，并带有来源（如打卡、活跃度、随机事件、小游戏、记忆考核）。
- `/我` 的“今日收益”只统计当天**正向**记账金额；扣款不会抵减该数字。
- “今日收益榜”同样仅按当天正向收益排名；只要当天有人有正向收益才会推送。默认推送时刻为 `12:00`、`16:00`、`20:00`、`23:59`，可在管理端调整。

## 3. 打卡、连续打卡与全勤

- `/打卡` 每个自然日只能成功一次。
- 默认打卡奖励为 `5` 摸鱼币，可配置。
- 连续打卡天数以今天开始向前连续计算；当天尚未打卡时，从昨天开始计算连续天数。
- 全勤按自然周结算：每周一结算上一周（周一至周日）。上一周七天均有成功打卡，自动获得全勤奖。
- 默认全勤奖为 `5` 摸鱼币，可配置；同一员工、同一自然周只会结算一次。

## 4. 日活跃度与日收益

- 非指令的普通发言会计入当天累计发言字符数；空白与空格不计入，斜杠指令不计入。
- 活跃度对玩家只展示为 `LV0` 至 `LV10`，不直接展示累计字数。
- 默认等级门槛与次日自动入账奖励如下；十个等级的门槛、奖励均可配置：

  | 活跃等级 | 累计字符数门槛 | 奖励（摸鱼币） |
  | --- | ---: | ---: |
  | LV1 | 10 | 1 |
  | LV2 | 25 | 2 |
  | LV3 | 60 | 3 |
  | LV4 | 90 | 4 |
  | LV5 | 140 | 5 |
  | LV6 | 190 | 6 |
  | LV7 | 250 | 7 |
  | LV8 | 330 | 8 |
  | LV9 | 410 | 9 |
  | LV10 | 500 | 10 |

- 每天零点后的后台任务结算前一自然日活跃度奖励，同一员工、同一天只会结算一次。

## 5. 商店与物品

- 管理员可以上架、停用和维护商品名称、描述、价格、库存。
- `/商店` 只负责展示已上架且有库存的商品，`/我的物品` 只负责展示库存。
- 当前版本尚未提供群内购买或使用物品指令；因此商店展示本身不会扣币或发放物品。

## 6. 随机事件（群内小剧场）

### 6.1 排期与场景

- 每天按固定时刻创建待开始场次。默认时刻为 `00:00`、`02:00`、`10:00`、`14:00`、`16:00`、`20:00`；场次数与时间均可在管理端配置。
- 固定的是推送时刻；每场使用的已启用场景与该场景内事件模板会随机选取并冻结到当日场次。
- 场景由以下内容组成：场景名称、报名公告、可报名身份及人数、事件名称、正式开场白、目标轮次与奖励。
- 报名公告按场景配置；正式开场白可使用身份变量（例如 `{主持}`、`{男1}`），实际发送时会替换为选择该身份的玩家昵称。
- 管理端可查看当日场次的“待开始 / 报名中 / 进行中 / 已结束”等状态，并可对待开始场次立即触发、调整时间、补充或移除今日场次。
- 同一群同时只允许一场随机事件处于报名中或进行中。若前一场跨天仍未结束，它会作为跨日进行中的场次保留；新的到点场次不会与它并行启动。

### 6.2 报名与进行

- 报名期默认持续 `15` 分钟，每 `5` 分钟提示一次剩余身份，二者均可配置。
- 玩家通过 `/加入 身份` 报名。身份容量已满、身份不存在、重复报名或事件已开始时不能加入。
- 所有席位满员后随机事件立即开始，机器人发送正式开场白。
- 进行中，参与玩家的普通发言会记录为事件详情并增加个人轮次；达到场景配置的目标轮次后，玩家可发送 `/退出` 领取该场景奖励。
- 未达到目标轮次而退出不能获得奖励；所有参与者都退出后，事件结束。
- 报名期退出会取消自己的报名；进行期退出后不能重新加入该场事件。
- 旁观者在事件进行中只能发送肉眼可见被中文全角括号 `（…）` 或英文半角括号 `(…)` 包住的内容；不符合格式会收到旁观警告。

### 6.3 随机事件优先级（硬性门禁）

- 报名中或进行中的随机事件拥有最高优先级，其他玩法和普通指令均不能打断它。
- 报名中默认放行 `/加入 身份` 与已报名玩家的 `/退出`；进行中默认放行参与玩家的普通剧情发言与 `/退出`。
- 管理员可分别配置“报名中允许执行的指令”和“进行中允许执行的指令”；现有任一群内指令均可被放行。被放行后会照常执行业务逻辑，例如放行 `/打卡` 后可正常打卡、入账与回复。
- 未被当前状态允许清单放行的指令或不合规发言均不会执行业务逻辑：不发奖励、不扣币、不创建玩法状态；机器人改为发送“随机事件拦截提示”。默认文案为“当前有随机事件发生，监事不会处理。”，管理员可在随机事件规则中编辑。
- 已加入玩家在进行中的正常剧情发言不属于指令拦截范围，仍会记录事件轮次；旁观者的合规括号发言同样不触发拦截提示。

## 7. 摸鱼躲猫猫（单人）

- 发起指令：`/开始摸鱼躲藏`；选择指令：`/躲 序号`。
- 系统从已启用的躲藏地点中随机展示 7 个候选地点（默认场景库含 10 个公司地点），玩家选择其中 1 个躲藏。
- 每人每天默认最多发起 `2` 次，可配置。若选择超时，默认 `2` 分钟后自动取消并返还该次次数，不扣币。
- 选择后系统进行两轮巡查：第一轮巡查 3 个地点；若未找到，再发送第二轮巡查 2 个剩余地点。每一轮分别发送消息。
- 默认规则为开局不扣费：被任一轮找到时扣除 `1` 摸鱼币；两轮都未找到时奖励 `3` 摸鱼币。惩罚金额、胜利奖励、每日次数、选择时限及场景库均可配置。
- 随机事件报名中或进行中时，不能发起躲猫猫；已存在待选择的躲猫猫时也不能重复发起。

## 8. 记忆考核

### 8.1 共通规则

- 记忆文本只由配置的字符集随机生成，默认包含大小写英文字母、数字和 `!@#$%&*_-`，不含空格和换行。
- 系统发送考题后按配置秒数撤回；**仅在机器人确认撤回成功后**才接受玩家作答。
- 玩家作答必须使用 `/答案 内容`；普通聊天和其他非玩法指令不参与答案判定，也不会计入答错。
- 同一时刻全群只允许一场记忆考核：单人挑战、对战等待中和对战进行中都占用该名额。
- 随机事件报名中或进行中时，不能发起或加入记忆考核。

### 8.2 单人挑战

- 发起：`/记忆考核`；默认每日限制 `1` 次，可配置。
- 默认共有 5 级：字符长度依次为 `5 / 7 / 9 / 11 / 13`，对应可收手奖励 `1 / 2 / 3 / 4 / 5` 摸鱼币；等级、长度、奖励和展示撤回秒数均可配置。
- 每级答对后，发送 `/继续` 挑战下一层，或发送 `/收手` 领取当前等级的对应奖励。
- 任一级答错会结束挑战，未收手的奖励全部失效；单人挑战没有额外失败扣币。
- 通过最高级会自动结算该级奖励。

### 8.3 双人对战

- 发起：`/记忆考核 对战`；另一位员工发送无参数的 `/加入` 入局。
- 发起后等待一名对手，不存在多个候选对局；满两人后才生成并展示考题。
- 默认使用第 5 级难度，双方各冻结 `5` 摸鱼币，基础奖池为 `10` 摸鱼币；难度、撤回秒数、基础奖池、错误冻结额、错误上限与作答时限均可配置。
- 撤回成功后，最先完整且准确发送 `/答案 内容` 的玩家赢得当前全部奖池。
- 每次答错，答错者默认再扣 `1` 摸鱼币加入奖池；达到默认 `10` 次答错即判定不合格。
- 一方达到答错上限只淘汰该玩家，另一位员工仍可继续作答；仅当双方都不合格时，系统才回收奖池。
- 玩家可发送 `/投降`；另一名仍合格玩家获得全部奖池。
- 若双方均不合格，或默认 `10` 分钟作答时限到期仍无人答对，系统回收奖池，不向任一玩家发奖。

## 9. 管理与数据一致性

- 所有经济配置、活跃度规则、收益榜时段、随机事件场景与排期、躲猫猫、记忆考核均由管理端维护；新增或修改配置只影响后续创建或结算，已开始玩法会使用创建时冻结的关键规则。
- 打卡、每日玩法次数、活跃度与全勤等自动结算均采用幂等记录；重复群消息或重复定时执行不应重复结算。
- 机器人使用独立浏览器用户目录保存平台登录状态；管理端人工登录使用三分钟操作租约，便于多人协作且支持其他管理员中断。

## 10. 职级、部门与晋升审批

- 职级与部门独立配置、独立存储。新员工默认职位为“实习生（LV1）”，默认部门为“未分配部门”。职位只代表群内玩法资格，绝不自动授予后台管理员权限。
- `/我` 或 `/me` 应展示昵称、职位和部门，以及既有的余额、今日活跃度、今日收益和连续打卡天数；`/职位` 展示所有启用职位及其晋升价格、按日均 90 摸鱼币估算的预计所需天数、投票权益、多人小游戏发起次数与群内管理资格。预计天数只作展示，不参与晋升结算。
- 部门预置：色色事业部、小游戏娱乐部、次元外联部、风纪监察部、核心技术部、摸鱼研究部、抽象艺术部、学院。`/部门` 仅展示启用部门及说明；新员工默认属于“未分配部门”。
- 部门可由管理端维护名称、说明和启用状态。“未分配部门”不可删除；已被员工使用的部门不可删除，停用部门不可再加入或切换。
- `/加入部门 部门名` 仅供未分配员工申请首次加入；`/切换部门 部门名` 仅供已有非默认部门的员工申请切换。两者均不即时变更部门：同一员工同一时刻最多一条待审部门申请，申请有效期为 24 小时，申请编号独立于晋升申请并递增不复用。
- 只有目标部门中、当前职位严格高于申请人的员工才能处理该申请，申请人不能审批自己；目标部门在申请后停用时，不得再同意转入。`/部门申请列表` 只显示本人可处理申请；`/同意部门 1 2 3`、`/全部同意部门` 同意后才更新部门；`/拒绝部门 1 2 3`、`/全部拒绝部门` 和过期均不变更部门。所有处理动作记录审批人、时间和结果；批量按项独立处理。
- 晋升不是自动升级，也不按累计收入计算。员工发送 `/晋升` 只能申请下一档普通职位；申请时不扣币，晋升同意时才从当前余额扣除对应摸鱼币。余额不足时提示“你的摸鱼币不够呢”，申请保持待审。
- 每名员工同一时刻最多有一条待审晋升申请，申请有效期为 24 小时。申请会冻结当前职位、目标职位与价格；同意、拒绝或过期后关闭，关闭后可重新申请。
- 只有当前职位严格高于申请人当前职位的员工才可审批，且不能审批自己。`/晋升申请列表` 只显示本人有权处理的申请；申请编号全局递增且不复用。
- `/同意 1 2 3`、`/全部同意` 按项重新校验有效期、申请状态、审批资格与余额；一项失败不回滚其他有效项。`/拒绝 1 2 3`、`/全部拒绝` 按同一权限范围关闭申请但不扣币。全部审批动作都记录审批人、处理时间与结果。
- 默认职位及价格为：实习生 LV1（0）、正式员工 LV2（80）、小组长 LV3（200）、副主管 LV4（500）、主管 LV5（500）、部门副经理 LV6（800）、部门经理 LV7（800）、部门副总监 LV8（1000）、部门总监 LV9（1000）、公司负责人 LV10（2000）。LV8 及以上仅展示“群内管理资格”，具体群管理命令须后续单独定义。
- 核心董事会（LvMax）不可通过 `/晋升` 获得，只能由超级管理员授予或撤销；撤销时职位回落至公司负责人 LV10，不退款且不改变部门。核心董事会默认投票权益为 10、多人小游戏发起次数不限。
